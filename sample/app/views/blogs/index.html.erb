<h1>ブログ一覧</h1>
<p><%= notice %></p>

 <!-- @blogsのみだと@blogsの中に入れるの情報があるということ
 しかわからないのでeachを使ってここの値を出力する  -->
<!-- <p>Find me in app/views/blogs/index.html.erb</p> -->
<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.title %></td>
    <td><%= blog.content %></td>
    <td><%= link_to '詳細を確認する', blog_path(blog.id) %></td>
    <td><%= link_to "ブログを編集する", edit_blog_path(blog.id) %></td>
    <td><%= link_to "ブログを削除する", blog_path(blog), method: :delete ,data: { confirm: '本当に削除していいですか？' } %></td>
  </tr>
<% end %>
</table>
<%= link_to 'ブログの新規作成', new_blog_path %>
